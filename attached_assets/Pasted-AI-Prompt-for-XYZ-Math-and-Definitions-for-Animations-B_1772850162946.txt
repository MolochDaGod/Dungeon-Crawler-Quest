AI Prompt for XYZ Math and Definitions for Animations
Based on your request, I've crafted a detailed AI prompt you can use (e.g., with tools like Grok, ChatGPT, or image/code generators) to produce mathematical definitions and formulas focused on XYZ coordinates. This is tailored for animating a character like the one in your image—voxel/pixel style, with actions like attacks, dodges, rolls, blocks, stuns, etc. I've assumed "XYZ math" refers to 3D coordinate-based mathematics (common in game dev for positioning, rotation, and movement in X, Y, Z axes), which is super useful for animation systems in engines like Unity, Godot, or even Web3-integrated ones.

The prompt is designed to output structured, actionable content: definitions, formulas, and examples that could be directly implemented in code (e.g., TypeScript or Rust for Solana). It emphasizes clarity, as per your core values, and includes safety for animations (like collision avoidance). You can copy-paste this prompt directly into an AI tool and refine from there.

The AI Prompt:
vbnet
Generate a comprehensive guide on XYZ math (3D coordinate mathematics) and definitions for creating animations of combat and movement actions in a voxel-based game character, similar to a Minecraft-style pixel art figure with brown hair, blue eyes, green tunic, and a bow on its back. Focus on actions like attacks (e.g., bow shooting or melee swing), dodge (quick sidestep), roll (evasive tumble), block (defensive stance with shield or arms), stun (temporary immobilization with visual effects), and similar mechanics (e.g., jump, parry, recovery).

Structure the output as a Markdown document with the following sections:

1. **Introduction to XYZ Math for Animations**: Explain core concepts like XYZ coordinates (X for horizontal, Y for vertical, Z for depth), vectors, quaternions for rotation, easing functions (e.g., linear, cubic bezier for smooth motion), and interpolation (lerp/slerp) for frame-by-frame animation. Include why this math is essential for realistic, efficient animations in 3D space.

2. **Key Definitions and Formulas**: Provide mathematical definitions and formulas for each action. Use LaTeX formatting for equations (e.g., $position = \begin{pmatrix} x \\ y \\ z \end{pmatrix}$). Cover:
   - **Attack**: Formulas for projectile trajectory (parabolic arc using gravity vector), swing animation (rotation around Y-axis with angular velocity ω).
   - **Dodge**: Quick translation in X/Z plane, with velocity vector and deceleration curve.
   - **Roll**: Combined rotation (around X-axis) and translation, including roll distance and recovery time.
   - **Block**: Static pose with collision bounding box adjustments in XYZ, plus stun resistance multiplier.
   - **Stun**: Temporary zero-velocity state with oscillation (small XYZ jitter) for visual effect, and duration formula based on impact force.
   - Additional: General formulas for hit detection (distance between XYZ points < threshold), animation blending, and state machines.

3. **Implementation Examples**: Give pseudo-code snippets in TypeScript (or Rust if Web3-focused) showing how to apply these in a game loop. For instance, animating a dodge: Calculate new position as current_pos + dodge_vector * delta_time.

4. **Best Practices for Animation Creation**: Tips on integrating with tools like Blender or Unity for voxel characters, optimizing for performance (e.g., avoid floating-point precision issues), and ensuring actions feel responsive (e.g., input lag compensation). Include how to adapt for Web3 (e.g., on-chain randomization of stun durations).

Ensure all content is clear, actionable, and includes diagrams (text-based or Mermaid syntax) for visualizing XYZ transformations. Keep it concise yet detailed, around 1500-2000 words, suitable for developers building animation systems.
This prompt should yield a solid, self-contained resource you can use or expand on. If you run it through an AI and get output, we could iterate on turning it into code templates or even generate images of the animated poses (leveraging your preference for quick image creation).