node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.vercel

# Generated by scripts/deployment-metrics.mjs (CI uploads it as an artifact)
deployment-metrics.json

.env
.env.local
.env.*.local
.local
.agents
.config

# Replit artifacts
.replit
replit.md

# Game server (Colyseus multiplayer)
game-server/node_modules
game-server/.env

# Large asset packs (managed locally, not in git)
public/assets/packs/
# Tileset assets (downloaded separately per dev machine)
public/assets/dungeon-tileset/
public/assets/tropical-tileset/
# Client-side asset packs (if any)
client/public/assets/packs/
.env*.local
